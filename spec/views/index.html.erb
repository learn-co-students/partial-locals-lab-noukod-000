<%= form_tag(students_path method: :get) do %>
  Search: <%= text_field_tag :search, params[:search] %>
  <%= submit_tag 'Search', name: nil %>
<% end %>

<% Students.each do |student| %>
   <% render partial: "student", locals: {classroom_student: @student} %>
<% end %>
